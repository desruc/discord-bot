interface NewChannelMeta {
  name: string;
  parent?: string;
}

export const categories: string[] = [];

export const textChannels: NewChannelMeta[] = [];

export const voiceChannels: NewChannelMeta[] = [];
